# Project dependencies
# https://www.npmjs.org/doc/misc/npm-faq.html#should-i-check-my-node_modules-folder-into-git
node_modules
.cache/
# Build directory
public/
static/admin/*.bundle.*
.DS_Store
yarn-error.log

# Claude Code local settings
.claude/

# Editor / IDE
.vscode/
.idea/
*.swp

# Env files (never commit secrets)
.env
.env.*
!.env.example

# Local integration notes that contain a live secret — keep out of git.
# Redact the x-api-key before ever committing this.
TRIAL_WEBHOOK_INTEGRATION.md

# Source thumbnails staging folder — copies live in src/img/indicators/
/thumbnails/

# Logs
*.log
npm-debug.log*
yarn-debug.log*

# OS junk
Thumbs.db
Desktop.ini
